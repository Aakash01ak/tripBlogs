<%- include("../partials/header") %>

<div class="container">
    <div class="row">
       <div class="col-lg-12">
          <div class="form3">
                   <h4><i id="logo3" class="fa fa-paper-plane"></i> Edit Comment</h4>
                    <form action="/blogs/<%= blog_id %>/comments/<%= comment._id %>/?_method=PUT" method="POST">
                        <p class="name1">Comment</p>
                        <textarea name="comment[text]"><%= comment.text %></textarea><br>
                        <input class="btn btn-danger" type="submit" value="Update">
                    </form>
           </div>
       </div>
   </div>
</div>

<%- include("../partials/footer") %>